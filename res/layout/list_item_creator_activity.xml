<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:id="@+id/list_item_creator_activity"
    tools:context="com.gp.app.professionalpa.layout.ListItemLayout" >

    <!-- <com.gp.app.professionalpa.compositecontrols.ListViewItemLayout
        android:id="@+id/list_item"
        android:layout_width= "300dip"
        android:layout_height="70dip"
        android:hint="@string/change_importance"/> -->
        
    <Button
        android:id="@+id/save_list"
        android:layout_width= "30dip"
        android:layout_height="30dip"
        android:hint="@string/change_importance"
        android:text="@string/save"
        android:layout_below="@id/list_item"/>

    <Button
        android:id="@+id/add_more_items"
        android:text="@string/add"
        android:layout_width="30dip"
        android:layout_height="30dip"
        android:layout_alignBaseline="@+id/save_list"
        android:layout_alignBottom="@+id/save_list"
        android:layout_marginLeft="30dp"
        android:layout_toRightOf="@+id/save_list"
        android:hint="@string/add_more_items"
        android:onClick="addNewListItem"/>
        
</RelativeLayout>